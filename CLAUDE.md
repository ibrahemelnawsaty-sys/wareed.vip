# تعليمات العمل على مشروع وريد

## قاعدة أولى: لا يُسلَّم شيء قبل اختباره وتحليله

**أي تعديل — طُلب الآن أو لاحقاً — يُختبر ويُحلَّل قبل رفعه.** لا يُدفع كود إلى
`main` لأنه «يبدو صحيحاً»؛ الموقع يخدم عملاء فعليين.

الحد الأدنى قبل أي `git push`:

1. `vendor/bin/pest` — كل الاختبارات خضراء، ومع كل ميزة اختبارها الذي يغطّي
   الحالة الطبيعية والحدود والتوافق مع البيانات القديمة.
2. `vendor/bin/pint --dirty` — نظيف.
3. **تشغيل فعلي في متصفّح** لأي تغيير مرئي: افتح الصفحة، نفّذ التفاعل، صوّرها،
   وافحص الصورة. الاختبارات لا تكشف تخطيطاً مكسوراً ولا زراً فارغاً.
4. **توليد PDF فعلي** لأي تغيير في عرض السعر أو مستند الطلب، وفحص عدد الصفحات
   وشكل كل صفحة.
5. **رندرة البريد فعلياً** لأي تغيير في القوالب، والتأكد من ظهور الشعار والروابط.

عند اكتشاف خطأ أثناء التحقق: يُصلَح ويُغطّى باختبار يمنع عودته.

## بنية المشروع

- **Laravel 13 · PHP 8.4 · Filament 5 · Livewire · Pest 4 · Pint**
- الواجهة عربية RTL بالكامل، والعربية هي لغة الزائر الجديد افتراضاً
  (`SetLocale` لا يقرأ `app.locale` لأن قيمته `en` في هيكل لارافل).
- ألوان الهوية: `#3b82f6` → `#8b5cf6` → `#2dd4bf`، والحبر `#0d1830`.

### نظام التصميم (لا يُخالَف)

- **الخط: «ثمانية» فقط**، مستضاف ذاتياً في `public/fonts/thmanyah/` (WOFF2)
  بخمسة أوزان. لا خدمة خطوط خارجية في أي صفحة. الأوزان بحسب النص:
  `900` للعناوين الكبرى والأرقام · `700` لعناوين الأقسام والبطاقات ·
  `500` للتسميات وروابط التنقّل والأزرار الثانوية · `400` للمتن. لا وزن سادس.
  البريد الإلكتروني استثناء: خطوط النظام (Tahoma/Segoe UI) لأن عملاء البريد
  لا يعتمدون على خطوط الويب.
- **الأيقونات: SVG خطّية موحّدة، ولا إيموجي في أي شاشة أو بريد.** الملصقة في
  `resources/views/partials/icons.blade.php` والاستخدام `<x-w-icon name="store" />`
  (الاسم `w-icon` لأن `x-icon` محجوز لحزمة blade-icons داخل Filament).
  أيقونات الخدمات ومميّزاتها مخزّنة في قاعدة البيانات كإيموجي — بيانات إنتاج
  لا تُعدَّل من هنا — فتُترجم عند العرض عبر `App\Support\Icons::name()`،
  وكل قيمة مجهولة تسقط على أيقونة افتراضية بدل مربّع فارغ.
- **لا حوافّ حادّة:** كل زاوية ≥ 14px (`--r-*`)، والأزرار كبسولية
  (`border-radius: 999px`)، والظلال منتشرة منخفضة التعتيم (`--sh-1`/`--sh-2`)،
  والانتقالات بمنحنى واحد `--ease`.

### مسار طلب المتجر
`app/Http/Controllers/QuoteController.php` هو قلب النظام: النموذج، المستند،
عرض السعر، مراحل الطلب، وقرار العميل.

- **المراحل الثماني:** `awaiting_meeting` ← `meeting_scheduled` ← `quote_due` ←
  `awaiting_approval` ← `awaiting_contract` ← `awaiting_requirements` ←
  `in_progress` ← `delivered`. العدّاد يعمل في `quote_due` و`in_progress` فقط.
  اعتماد العميل للعرض ينقله تلقائياً إلى `awaiting_contract` ويُنشئ مسوّدة
  العقد، واعتماده لجميع بنود العقد ينقله إلى `awaiting_requirements` (رفع
  ملفات المشروع)، وأول رفعة تنقله فعلياً إلى `in_progress` وتُشغّل عدّاده — أو
  يبدأ الفريق التنفيذ يدوياً من اللوحة بلا انتظار عقد ولا رفع. الترتيب يُقارَن
  عبر `QuoteController::stageReached()` لا بأرقام ثابتة، فالطلبات القديمة
  المخزّنة بمفتاح المرحلة تبقى صحيحة عند إدراج مرحلة جديدة.
- **الخدمات الثلاث على المسار نفسه:** `app/Support/ServiceFlow.php` هو مصدر
  مفردات كل خدمة (`ecommerce` / `tech_solution` / `training`): تسميات المراحل
  ونصوص العميل، عنوان مستند الطلب والعقد، تسمية «اسم المتجر/الجهة»، ونصوص
  رفع المتطلبات. المفاتيح والآليات والتخزين والمسارات واحدة — لا ثلاث آلات
  حالة. أي نوع آخر يُعامل كمتجر (توافق خلفي). الطلبات تصل من نموذج عرض السعر
  (متاجر) أو نماذج صفحات الخدمات (`source = service_{key}`، وإجاباتها بأسماء
  حقول `form_fields` تُترجم لعناوينها عبر `ServiceFlow::answers()`). قوالب
  البريد لكل خدمة صياغتها: `MailTemplates::VARIANTS[type][stage]` بمفاتيح
  حفظ `mail_tpl_{type}_{stage}_{part}`، وقوالب المتاجر بمفاتيحها الأصلية،
  والمرحلة الغائبة من الصياغة تقع على نص المتاجر.
- **نظام العقود:** `app/Support/Contracts.php` (دورة الحياة) و`ContractTemplate`
  (البنود المقترحة) و`ContractNumber` (عدّاد عام في صفّ `contract_sequence`
  بجدول الإعدادات، بقفل صفّ داخل معاملة) و`ContractController` (صفحة العميل
  `contracts/review.blade.php`) و`Filament\Pages\Contracts` (اللوحة). يُخزَّن
  في `payload['_contract']`: الرقم `WRD-CTR-YYYY-MM-DD-00001`، الحالة
  `draft|sent|feedback|approved`، الجولة، والبنود بقرار العميل على كل بند
  (`approved|edited|deleted` + ملاحظته). **قاعدة القفل:** البند المعتمد يبقى
  مقفلاً في الجولات التالية ولا يُطلب قراره، وكل بند غير معتمد يُصفَّر قراره عند
  إعادة الإرسال، وتعديل الفريق لنص بند — ولو معتمداً — يُصفّر قراره. العميل
  يقرّر في كل بند صراحةً ثم يظهر له زر «اعتماد مسوّدة العقد» إن لم يعترض على
  شيء، وإلا زر «إرسال ملاحظات العقد»؛ والخادم يستنتج النتيجة من القرارات
  نفسها لا من الزر. الختم واسم الموقّع وصفته إعدادات موقع دائمة
  (`contract_stamp` على القرص `local` برابط مؤقّت، `signer_name`،
  `signer_title`) تظهر في كل عقد. مقسّم طباعة العقد يقرأ الوحدات من كل
  `data-pg-unit` بترتيب المستند، فكل قسم جديد يحمل السمة يُطبع تلقائياً.
  **النسخ الموقّعة** بعد الاعتماد: `_contract['signed']['company'|'client']`
  ملفات على القرص `local` تحت `contracts/{رقم العقد}/`؛ ترفع الشركة نسختها من
  صفحة العقود وترسلها للعميل مرفقة ببريد `contract_signed_sent`، ويرفع العميل
  نسخته من صفحة العقد نفسها (`quote.contract.copy[.signed]`) فيُشعَر الفريق،
  والحالة `signing_label` مشتقة من وجود الملفين لا من حقل مستقل.
- **متطلبات المشروع:** ملفات يرفعها العميل بعد الاعتماد (هوية بصرية، بيانات
  منتجات...) من صفحة عرض السعر نفسها، بوصف اختياري لكل ملف. تُخزَّن في
  `payload['_requirements']` وفعلياً على قرص `local` الخاص (لا `public`، فلا
  اعتماد على `storage:link` غير مضمون على الاستضافة)، وتُنزَّل من اللوحة
  برابط موقّع مؤقّت عبر `Storage::disk('local')->temporaryUrl()`.
- **تتبّع فتح العرض:** كل زيارة لصفحة عرض السعر (المنصة) وكل فتح لبريد إصدار
  العرض (بكسل شفاف 1×1 على `/quote/track/{serviceRequest}`) تُسجَّل في
  `payload['_views']`، وأول مشاهدة من كل قناة فقط تُرسل إشعاراً لوريد — تجنّباً
  لإزعاج بريد عند كل تحديث صفحة. تتبّع فتح البريد أثر معروف الحدود (فحص روابط
  الأمان أو معاينات الروابط قد تُطلق مشاهدة زائفة) لا حل موثوق كامل له.
- **لوحة المتابعة:** `app/Filament/Pages/QuoteRequests.php`.
- **صفحة متابعة الطلب:** لعملاء الروابط المخصّصة عبر `/quote/{slug}`، ولبقية
  العملاء (الخدمات الثلاث) عبر الرابط الموقّع `quote.status.signed` — وهو ما
  يعيده `QuoteController::statusUrl()` ويُضمَّن في كل بريد كـ`{رابط_الطلب}`.
  الصفحة واحدة (`quote/status.blade.php`) وعناوينها ومفرداتها من `ServiceFlow`.
- **قوالب البريد الإلكتروني:** `app/Support/MailTemplates.php` — أربع عشرة
  رسالة بنصوص قابلة للتعديل من اللوحة (منها خمس للعقد: `awaiting_contract`
  و`contract_draft_sent` و`contract_resent` و`contract_approved`
  و`contract_signed_sent` الذي يحمل النسخة الموقّعة مرفقة)، ورسالة `received`
  يرسلها `ServiceRequestObserver` فور وصول طلب أي خدمة من الثلاث (طلبات التواصل
  العامة تصلها رسالة الاستلام العامة `ServiceRequestConfirmation`)، وكل زر ينقل
  مرحلة يرسل بريدها للعميل؛ وإشعارات الفريق الداخلية (قرار العميل على العقد
  وملاحظاته) نصوص ثابتة عبر `StageMessage` كبقية إشعارات الفريق. قالب
  `awaiting_approval` استثناء: لا يُرسَل تلقائياً عند أي انتقال مرحلة، بل زرّ
  «إرسال تذكير» في صفحة الطلب يرسله يدوياً وقت الحاجة (تذكير بعرض السعر
  المعلَّق) دون أن ينقل مرحلة الطلب.
- **إصدار العرض (Version):** `payload['_quote']['version']` يبدأ من 1 ويزيد
  تلقائياً في `QuoteRequests::issueQuote()` مع كل إصدار فعلي (`send: true`، أو
  أول عرض يُنشأ) — لا عند «حفظ دون إرسال». يظهر للعميل في صفحة عرض السعر
  (بجانب الرقم المرجعي) وفي بريد إصدار العرض، مع تنويه صريح من الإصدار الثاني
  فصاعداً («عرض مُحدَّث» + سياق طلب التخفيض إن كان قرار العميل الحالي
  `discount`) وسابقة «(مُحدَّث)» في عنوان البريد. العروض الصادرة قبل هذا
  التغيير تُقرأ كإصدار أول (توافق خلفي). قسم التنويه في `quote/proposal.blade.php`
  مسجَّل ضمن `UNITS_BEFORE_ITEMS` في مُقسِّم الطباعة — أي قسم جديد هناك يجب
  تسجيله في تلك المصفوفة وإلا اختفى صامتاً من نسخة PDF.

## قيود تشغيلية لا تُخالَف

- **بلا هجرات قاعدة بيانات.** النشر يدوي أو عبر Git بلا `artisan migrate`
  مضمون، فكل بيانات عرض السعر والمراحل والقرار تُحفظ داخل عمود
  `service_requests.payload` (`_quote`، `_flow`، `_decision`) أو في جدول
  الإعدادات. أي حقل جديد يتبع النمط نفسه.
- **بلا حزم جديدة.** لا `composer require` — النشر لا يشغّل `composer install`.
  توليد PDF يتم بالطباعة من المتصفّح، وQR عبر `chillerlan/php-qrcode` الموجود
  أصلاً ضمن Filament.
- **توافق خلفي إلزامي.** العروض الصادرة سابقاً يجب أن تظل تُقرأ وتُطبع بقيمها
  كما هي عند تغيير أي بنية بيانات (مثال: `notes` صارت مصفوفة وتقرأ النص
  المفرد القديم، و`discount` صار نسبة ويشتقّها من القيمة القديمة).
- **الأسرار لا تدخل Git إطلاقاً.** `MAIL_PASSWORD` و`DB_PASSWORD` وبيانات FTP
  تبقى في `.env` على الخادم.
- **الإعدادات مترجَمة**: `Setting::set` يحفظ باللغة النشطة وقت الحفظ، ولذلك
  `Setting::get` يرجع بالترتيب: لغة الزائر ← العربية ← الإنجليزية ← أي ترجمة
  محفوظة. بدون هذا الرجوع يختفي الإعداد كلياً لزائر بلغة أخرى. لوحة التحكم
  لا تمرّ بـ`SetLocale` فتحفظ بلغة `app.locale` (الإنجليزية)، ولذلك تقرأ
  `ManageSettings` الحقول غير المترجمة عبر `Setting::get` لا عبر ترجمة بعينها —
  وإلا ظهرت فارغة عند إعادة الفتح ثم حُفظت فارغة فوق قيمتها.
- **مقسّم الطباعة يقيس قبل تطبيق أنماط الطباعة**: `beforeprint` يسبق تفعيل
  `@media print`، فمقاسات `body.paged .pg` (عرض 210mm وارتفاع 297mm) تعيش
  خارج كتلة الطباعة عمداً — لو دخلتها لقاس المقسّم صفحاتٍ بارتفاع صفر وطبع
  عرض السعر ناقصاً بلا إنذار.

## البريد الإلكتروني

- المرسِل دائماً `info@wareed.vip` من إعداد `contact_email`.
- **كل بريد صادر لعميل يحمل نسخة CC إلى `contact_email`** عبر المستمع
  `App\Listeners\CopyCompanyOnOutgoingMail` (يُكتشف تلقائياً من مجلد
  `app/Listeners`)، والرسائل الموجّهة أصلاً إلى بريد وريد لا تُنسخ إليه مرة أخرى.
- القوالب في `resources/views/emails/` بجداول متداخلة ووضع فاتح مثبَّت،
  والشعار **PNG** لأن Gmail وOutlook لا يعرضان SVG.
- **فشل الإرسال يُسجَّل عبر `report()` ولا يُسقط العملية** — نقل المرحلة وحفظ
  الطلب وقرار العميل تنجح ولو تعطّل البريد.

## صياغة النصوص

- تُكتب «البريد الإلكتروني» كاملة، لا «البريد» وحدها.
- الأرقام والمبالغ والتواريخ عربية الصياغة، والمعرّفات اللاتينية `dir="ltr"`.
